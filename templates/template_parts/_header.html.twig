{# <header> #}

<style>
.search-container{
    width:40%;
}
.connexionBtn{
  color:white;
  text-decoration:none;
}

.navbar{
  height:10vh;
}
</style>
<nav class="navbar navbar-expand-lg" style="background-color: #e3f2fd;">
 <div class="container-fluid">
    <a class="navbar-brand" href="{{path('app_post')}}">Blog</a>
      
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">

     <div class="container-fluid search-container">
     {{ render(controller('App\\Controller\\MainController::search')) }}
    </div>

    <ul class="navbar-nav">
         {% if app.user and 'ROLE_ADMIN' in app.user.roles %}
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{path('admin')}}">Admin</a>
        </li>
         {% endif %}
          <li class="nav-item">
            <a class="nav-link active" aria-current="page" href="{{path('app_profil')}}">Mon profil {% if app.user %}
            ({{ app.user.firstname }})
          {% endif %}
      </a>
          </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{path('app_post')}}">Articles</a>
        </li>
      </ul>
      {% if app.user %}
        <button type="button" class="btn btn-dark" href="{{ path('app_auth', {'login':'true'}) }}"><a class="connexionBtn" href="{{path('app_logout')}}">Déconnexion</a></button>
      {% else %}
        <button type="button" class="btn btn-dark" href="{{ path('app_auth', {'login':'true'}) }}"><a class="connexionBtn" href="{{path('app_auth')}}">Connexion</a></button>
     {% endif %}
    </div>
  </div>
</nav>
{# </header> #}